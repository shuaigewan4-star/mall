apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mall-portal
  namespace: mall-prod
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: mall-portal

  namespaceSelector:
    matchNames:
      - mall-prod

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
