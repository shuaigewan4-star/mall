pipeline {
    agent {
        label 'jenkins-agent'
    }

    stages {
        stage('check workspace') {
            steps {
                container('maven') {
                    sh '''
                     helm upgrade mall-admin ./mall-helm/mall-admin -n mall-prod
                    '''
                }
            }
        }
    }
}
